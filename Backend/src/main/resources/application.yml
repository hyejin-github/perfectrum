spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://j7c105.p.ssafy.io:3306/S07P22C105?serverTimezone=UTC&useUniCode=yes&characterEncoding=UTF-8&amp
    username: ssafy
    password: ssafy

  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org:
      springframework:
        web: debug
    sql: debug
server:
  port: 8083